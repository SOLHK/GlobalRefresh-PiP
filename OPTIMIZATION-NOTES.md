# 1.1.1-beta2 节能测试版

基于 SOLHK/GlobalRefresh-PiP 的 97b09f5。保留 CaiWanFeng / Yoroin 署名和 NOTICE。

- 使用现有公开 protected-data 通知：锁定后停止 PiP、主高刷驱动、播放看门狗、内容刷新、静音音频及有限后台任务。
- 取消待执行启动重试，阻止锁定/严重过热时的启动以及迟到的 PiP 启动回调。
- 严重/危急热状态停止会话。解锁或降温后需手动重新开启，不额外保活以等待自动恢复。
- PlayerLayer 播放看门狗从 30Hz DisplayLink 改为容许 0.2 秒合并的 1 秒 Timer。它本来只每秒检查播放状态，视频帧率与高刷请求不变。
- 更新应用版本、构建号、首页暂停状态及中英更新日志。

限制：protected-data 通知不是即时熄屏通知，密码设置、系统版本可能影响或延迟通知。不能保证按下锁屏键瞬间停止，也不能保证无密码设备的行为。没有真机帧率、功耗或温度测量；不保证全局120帧或省电比例。

真机验证：启动→隐藏→切换其他App；锁屏等待后检查会话停止；解锁手动重启；两种引擎分别重复；启动期间锁屏后不能被重试拉起；正常手动关闭仍可重启。相同亮度、应用内容、温度下比较旧版耗电及流畅度。
