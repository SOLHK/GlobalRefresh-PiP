# 1.1.1-beta4 节能修复测试版

本轮修复：

- `updateHomeView()` 原本可以重新创建锁屏刚停止的运行时间计时器。现在只有首页可见、应用活跃且未锁屏/过热时才允许计时刷新。
- 后台和离开首页时不再每秒重建首页 SwiftUI 内容。回到首页由原始开始时间补算经过时间，避免暂停刷新后显示落后。此计时仍表示会话经过时间，不代表屏幕实际高刷时间。
- PlayerLayer 播放检查若先于锁屏通知看到受保护数据不可用，统一走锁屏暂停路径，保留解锁恢复意图。用户主动停止仍取消恢复。
- AVPlayer 正在等待播放时不再每秒重复调用 `play()` 和安排音频释放；只在意外暂停时进行恢复。
- 新增 5 项生命周期回归测试。测试注入通知/状态，不能替代物理锁屏、设备发热及屏幕刷新率验证。

真机验收状态：尚未执行。本次运行环境为 Linux，无已连接 iPhone，也无已配置远程真机服务。GitHub Actions 的 iPhone SDK 编译及 iOS Simulator 测试不计作真机测试。

真机验收方法见 [DEVICE-VALIDATION.md](DEVICE-VALIDATION.md)。在完成对照测试前不宣称耗电降低比例、温度下降或其他应用稳定达到 120Hz。

## 继承的 beta3 行为及限制

基于 beta2，保留 CaiWanFeng / Yoroin 署名。

锁屏暂停高刷驱动、内容刷新、播放和静音音频，但保留已建立的 PiP 容器。收到解锁通知时自动恢复，系统停止 PiP 后允许一次有界重启，回到应用再次尝试。自动重启成功恢复原高度，用户操作覆盖自动恢复意图。严重过热仍停止会话，不自动重新启动。

不新增后台轮询或音频保活。公开 protected-data 通知不是即时熄屏信号。iOS 挂起/结束应用或禁止后台启动 PiP 时无法保证解锁自动恢复。保留容器不代表锁屏零耗电；视频暂停也可能导致系统撤销容器。源码和编译检查无法代替真机验证。

回归：亮屏隐藏→锁屏→解锁；长时间锁屏；锁屏期间系统结束PiP；启动过程锁屏；手动关闭后不能自动拉起；严重过热后不自动拉起；默认VideoCall与PlayerLayer分别测试。
