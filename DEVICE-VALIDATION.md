# beta4 真机验收

状态：未执行。不要把编译成功、模拟器 XCTest 成功或 App 内 FPS 数值写成真机全局 120Hz 的证据。

## 设备及测量

使用支持 ProMotion 的实体 iPhone，记录机型、系统版本与构建号、App 版本及构建号。使用本人签名后的 IPA。调试时可使用连接实体设备的 Mac/Xcode；本仓库目前没有可用的远程真机测试连接。

固定亮度、网络、环境温度、低电量模式和系统限制帧率设置，关闭录屏。充电会影响温度，耗电对照应拔掉充电线，待设备恢复相近热状态后开始。分别对比关闭本 App、beta3、beta4。每种条件使用相同应用、同样操作时长（建议至少 20 分钟），各重复 3 次并轮换顺序。记录起止电量、热状态、实际用时；百分比电量分辨率较低，小差异不能证明改进。

外部高速摄影可辅助检查目标应用滚动时的帧间变化；有调试权限时可用 Instruments 检查目标进程表现。App 内 CADisplayLink 计数只能描述本进程回调，不能直接证明其他 App 或物理屏幕全局稳定 120Hz。

## 验收矩阵

分别对默认 VideoCall 和 PlayerLayer 路线执行：

| 场景 | 核对结果 |
| --- | --- |
| 开启、隐藏浮窗，切到其他 App | 浮窗继续工作；首页运行时间计时器应停止，不应每秒更新后台首页 |
| 锁屏 1 分钟、5 分钟、20 分钟后解锁 | 记录真实锁屏信号时间、PiP 是否被系统回收、是否自动恢复以及延迟 |
| 锁屏恢复后手动关闭，再锁屏解锁 | 不应自行重新开启 |
| 开启过程中立即锁屏 | 取消启动任务，解锁后仅有界恢复，不循环重启 |
| 切换版本页/帧率页，再返回首页 | 首页计时刷新停止后能按开始时间补算，无累计误差 |
| 其他 App 抢占 PiP、来电/音频打断 | 无反复抢占、重启或持续重复播放操作 |
| 系统报告严重热状态 | 停止高刷会话；冷却后不擅自重启。不要人为加热设备 |
| 不设密码、延迟锁定、常亮显示 | 单独记录锁屏通知是否延迟/缺失；不能假定数据保护等同于即时灭屏 |

调试日志只在故障复现时打开并导出；正式耗电对照关闭调试，避免采样本身影响功耗。现有「耗电辅助日志」可记录环境和会话信息，无法替代系统能耗或跨应用帧率测量。

## 记录模板

设备/系统构建：
App 版本/构建：
日期、环境温度、亮度、网络、低电量/限制帧率设置：
路线、浮窗高度、保活模式：
对照顺序与每段起止电量、温度/热状态、时长：
锁屏/解锁时间、恢复成功与延迟：
证据（原始日志、测量文件）：
结论及未验证项：
